/**
 * Canonical Gateway Docker image for the public install guide.
 * Single CTA — no per-arch choice, no GitHub Release fallback.
 */
export const GATEWAY_IMAGE_DOWNLOAD_URL =
  "https://ferry-agent.aperture-agency.org/bundle/ferry-agent-gateway.tar";
